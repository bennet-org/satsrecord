{
  "name": "satsrecord",
  "private": true,
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter @satsrecord/web dev",
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "start": "node apps/web/dist/server/entry.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "typescript": "^6.0.3"
  }
}
